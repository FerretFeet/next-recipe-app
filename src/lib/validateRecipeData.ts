import { IRecipe } from "./utils/interfaces";

export const validateRecipeData = ({
  name,
  user_id,
  description,
  instructions,
  prep_time,
  cook_time,
  serving_size,
  rating,
  img,
  ingredients,
  tags,
}: IRecipe) => {
  return false;
};
